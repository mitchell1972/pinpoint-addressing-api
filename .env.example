# Copy to .env for local dev. All vars are prefixed PINPOINT_.
# Connection string for the dev database created by docker-compose.
PINPOINT_DATABASE_URL=postgresql://pinpoint:pinpoint@localhost:55432/pinpoint
PINPOINT_ENV_NAME=dev
